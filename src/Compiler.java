/**
 * Compiler: feeds lines into the parser and writes parser output
 * to output file. i.e. handles IO and nothing else.
 */
public class Compiler {
    public static void main(String[] args) {
        // Go through and create hashmap of variables & labels
        // Go through again and interpret lines

        //for line : lines
        //  parser.read(line) // look for symbols and populate hashmap

        //for line : lines
        //  binary = parser.convert(line) // turn the line into lines
        //  out.addln(binary)
    }
}